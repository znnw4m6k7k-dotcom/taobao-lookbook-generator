# Taobao Lookbook Generator - 修改总结

## 最新更新（2026-08-19）

### 新增完整工作流程文档

**新增文件**: `references/workflow-stages.md`

这是一份详尽的技术文档，描述了三种生成模式的完整工作流程，包括：

1. **预生成阶段**（Pre-generation Phase）
   - 源素材验证流程
   - 模式配置和参数验证
   - 特征提取详细步骤（三个锁的构建）
   - 镜头计划分配
   - 成本估算和预算管理

2. **生成阶段**（Generation Phase）
   - **标准/高级模式**：逐张生成 → 审核 → 重试循环
   - **自定义场景模式**：
     - 阶段1：生成16张基础棚拍图
     - 阶段2：场景版本生成（选择基础图 → 构建场景集成提示词 → 生成 → 审核）
   - 姿势选择策略
   - 提示词构建模板
   - 审核和重试逻辑

3. **后生成阶段**（Post-generation Phase）
   - 最终计数统计（按模式分类）
   - 成本日志格式
   - 任务清单（job-manifest.json）完整结构
   - 交付总结显示格式
   - 目录结构示例

**为什么需要这份文档**：
- 原有的 SKILL.md 主要描述"做什么"，但缺少"怎么做"的详细步骤
- 自定义场景模式引入了多阶段工作流，需要更清晰的流程说明
- 开发者和用户都需要理解完整的执行逻辑，特别是审核、重试、失败处理的机制

**文档特点**：
- 分阶段、分步骤的结构化说明
- 包含 JSON 示例（job-manifest.json 完整结构）
- 明确自动化边界（何时需要用户介入，何时自动运行）
- 包含标准模式和自定义场景模式的对比流程

**其他更新**：
- 在 SKILL.md 中添加了对 workflow-stages.md 的引用
- 在中文使用说明中添加了"完整流程详解"章节，引导用户查看完整技术文档
- 在中文使用说明的常见问题中新增4个自动化相关问题

## 主要改进

### 1. 精确的特征提取系统

**之前的问题**：
- 三个"锁"只是分类列表（如"face geometry, hair, skin tone"）
- AI无法从抽象词汇中真正锁定具体特征
- 每次生成时特征描述不一致，导致人物/服装变化

**现在的解决方案**：
- 新增 `references/feature-extraction-guide.md`，详细指导如何从参考图中提取特征
- 三个锁现在包含**详细的、可直接插入提示词的自然语言段落**
- 例如：不再是"eyes"，而是"Eyes are hazel-green with visible green undertone, almond-shaped, medium size, naturally spaced, with subtle hooded eyelids and medium-length dark lashes"

**具体改进**：
- `identity_lock`：15+个面部特征的详细描述，包括脸型、五官比例、发色发型、肤色肤质、身材比例、表情范围
- `product_lock`：逐区域的服装构造描述，包括精确颜色、面料质感、领口深度、袖长、腰线处理、下摆位置、图案细节、五金配件等
- `styling_lock`：每件配搭单品的完整描述，包括鞋子的具体款式、颜色、材质、细节

### 2. 参考图角色明确标注

**新增步骤**：在预检阶段要求明确标注每张源图的角色：
- `model_face_primary`：最清晰的正面脸部参考
- `model_body_primary`：全身比例参考
- `model_face_side` / `model_face_back`：侧面/背面参考
- `product_front` / `product_back`：服装正反面
- `product_detail_N`：细节图（编号）
- `styling_complete_front` / `styling_complete_back`：完整造型
- `pose_reference_N`：姿势参考（如果提供）

**好处**：
- 生成时每张参考图的用途明确
- 避免混淆（如把造型图当产品图，导致颜色错误）
- 便于冲突解决（产品图优先于造型图）

### 3. 自动化的二次审核流程

**之前的问题**：
- 文档提到 `$taobao-lookbook-reviewer` 但没说明如何使用
- 没有明确的审核标准
- 用户不知道何时需要介入

**现在的解决方案**：
- 新增 `references/reviewer-spec.md`，完整定义审核标准
- **7个评估维度**：身份一致性、产品准确性、造型合规、真实感质量、格式合规、拍摄覆盖、系列一致性
- **A/B/C分级系统**：
  - A（通过）：自动移入 `final/`
  - B（轻微问题）：自动重新生成，最多3次重试
  - C（重大问题）：自动重新生成，最多3次重试
  - 第3次重试仍为B/C：自动放入 `human-review/`，继续下一张

**完全自动化**：
- 用户无需介入，除非素材不足
- 审核→重试→再审核的循环全自动
- 只有在3次重试后仍失败的图片才需要人工检查

### 4. 灵活的配置系统

**新增 `references/configuration-options.md`**，支持：
- **图片数量调整**：默认16张，可配置8-24张
- **重试次数调整**：默认3次，可配置1-5次
- **成本上限**：可设置预算上限（如$50），超出时提示用户
- **批次暂停点**：可选择在特定图片后暂停确认
- **预设模式**：快速模式、详尽模式、纯影棚模式、预算限制模式

**成本估算**：
- 生成前显示预估成本（假设平均每张1.5次生成）
- 实时追踪实际成本
- 达到预算上限时自动停止

### 5. 输出格式严格约束

**新增明确的负面清单**（每次生成时插入）：
- 不要生成拼图、网格、对比图、多人图
- 不要添加边框、标签、箭头、水印、logo
- 不要包含参考图缩略图
- 必须是单张3:4竖版图，包含一位完整模特

### 6. 冲突解决规则明确化

**优先级顺序**：
1. 产品正反面/细节图 → 定义产品本身
2. 完整造型图 → 定义配搭
3. 模特参考图 → 仅定义人物
4. 已生成的图 → 仅作连续性参考，不覆盖原始素材

**实际应用**：
- 产品图显示蓝色，造型图显示绿色 → 使用蓝色
- 产品图显示及膝，造型图显示小腿 → 使用及膝
- 某细节在一张图中可见、另一张图被遮挡 → 使用可见证据

### 7. 姿势参考的正确使用

**明确说明**：
- 姿势参考图**只用于**：姿势骨架、身体朝向、重心分配、肢体位置
- **不复制**姿势参考图中的：脸、发型、身材、服装、鞋子、肤色、表情、背景

**避免僵硬**：
- 如果完全复制会导致身体僵硬或变形，优先使用自然的lookbook姿势，同时保留相同的主要重心和身体角度

### 8. 素材不足时的处理

**新增明确的停止协议**：
```
[特征类别] is not clearly visible. 
Visible evidence shows: [可以看到什么]. 
Hidden or unclear: [无法确认什么]. 
REQUEST: [需要的具体视图 - 如"产品背面图，显示下摆和拉链"]
```

**不猜测、不推断、不使用"典型"替代**

### 9. 交付状态完整记录

**job-manifest.json 现在包含**：
- 所有源图的标注角色
- 三个详细锁的完整段落（可直接用于提示词）
- 每张图的shot ID、pose ID、生成提示词
- 逐轮尝试记录、审核评分和反馈
- 最终结果和失败原因
- 成本日志

**后处理总结**：
- A级图片数量（目标：16）
- 如果少于16：列出缺失的shot ID和失败原因
- 总生成调用次数和估算成本
- `human-review/` 路径（如果有需要人工检查的图片）

## 与 huang-duan-fa 的对比优势

| 特性 | huang-duan-fa | taobao-lookbook-generator |
|------|---------------|---------------------------|
| 模特身份 | 固定的黄短发模特 | **支持任意模特**，每次自动提取特征 |
| 特征锁定 | 手写的详细描述 | **自动从参考图提取**详细描述 |
| 审核机制 | 用户手动确认 | **自动审核+重试循环** |
| 图片数量 | 灵活 | 可配置（8-24张） |
| 成本控制 | 无 | **预估成本+预算上限** |
| 批量处理 | 单次处理 | 系统化16张+自动重试 |

## 实际使用流程示例

1. **用户提供素材**：
   - 模特照片（正面、侧面、背面）
   - 服装照片（正面、背面、细节图）
   - 完整造型照片

2. **系统自动处理**：
   - 标注每张图的角色
   - 提取详细特征描述
   - 生成三个详细锁
   - 检查素材完整性

3. **如果素材不足**：
   - 停止并明确说明缺少什么（如"需要产品背面图，显示后领和拉链"）
   - 用户补充后继续

4. **自动生成16张图**：
   - 每张图：生成 → 审核 → 如果B/C则重试（最多3次）→ A则进入final/
   - 完全自动，无需用户介入
   - 实时追踪成本

5. **交付**：
   - `final/`：所有A级图片（目标16张）
   - `human-review/`：3次重试后仍失败的图片（如果有）
   - `job-manifest.json`：完整记录
   - 成本报告

## 文件清单

新增的文档：
1. ✅ `references/feature-extraction-guide.md` - 特征提取详细指南
2. ✅ `references/reviewer-spec.md` - 审核标准和分级系统
3. ✅ `references/configuration-options.md` - 配置选项说明

修改的文档：
1. ✅ `SKILL.md` - 主文档，整合所有改进

## 下一步建议

1. **测试实际效果**：用真实素材跑一遍完整流程
2. **调整审核标准**：根据实际生成质量微调A/B/C阈值
3. **优化成本估算**：根据实际使用情况调整平均重试率（当前假设1.5）
4. **扩展pose库**：如果12个pose不够用，可以扩展到20-24个
5. **多色款式批处理**：未来可以考虑同一款式多色的批量处理功能
